rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Make sure user authenticated before writing
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
